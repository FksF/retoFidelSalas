export enum InputTypes {
  email = 'email',
  number = 'number',
  tel = 'tel',
  text = 'text',
  password = 'password',
}

export enum HttpStatusCodes {
  BadRequest = 400,
  Unauthorized = 401,
  NotFound = 404,
  Conflict = 409,
  InternalServerError = 500,
}
